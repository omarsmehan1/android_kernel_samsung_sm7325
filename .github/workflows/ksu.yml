name: 🚀 Build NovaKernel KSU

on:
  workflow_dispatch:
    inputs:
      device:
        description: 'اختر الجهاز المراد بناء الكيرنل له'
        required: true
        default: 'a73xq'
        type: choice
        options: [a73xq, a52sxq, m52xq]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: 📥 جلب الكود المصدري (Checkout)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🛠️ تشغيل سكربت البناء
        run: |
          chmod +x build_ksu.sh
          ./build_ksu.sh ${{ github.event.inputs.device }}

      - name: 🏷️ تحديد اسم الملف (Artifact Name)
        run: |
          echo "ARTIFACT_NAME=NovaKernel-${{ github.event.inputs.device }}-KSU-$(date +%Y%m%d)" >> $GITHUB_ENV

      - name: 📤 رفع الكيرنل الجاهز
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          # السر هنا: رفع ما بداخل المجلد مباشرة يمنع الضغط المزدوج
          path: ~/toolchains/RIO/work_ksu/*
