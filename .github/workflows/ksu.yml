name: 🚀 Build NovaKernel KSU Professional

on:
  workflow_dispatch:
    inputs:
      device:
        description: 'اختر الجهاز المراد بناؤه'
        required: true
        default: 'a73xq'
        type: choice
        options: [a73xq, a52sxq, m52xq]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: 📥 جلب الكود المصدري (Checkout)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 تثبيت الأدوات والاعتمادات (Dependencies)
        run: |
          chmod +x build_ksu.sh
          ./build_ksu.sh deps

      - name: 🛠️ تحميل سلسلة أدوات Clang (Toolchain)
        run: ./build_ksu.sh tools

      - name: 🧬 إعداد KernelSU و SUSFS
        run: ./build_ksu.sh ksu

      - name: 🏗️ بناء النواة (Kernel Compilation)
        run: ./build_ksu.sh build ${{ github.event.inputs.device }}

      - name: 📦 تجهيز حزمة التفليش (Packaging)
        run: ./build_ksu.sh pack ${{ github.event.inputs.device }}

      - name: 🏷️ تحديد اسم الملف (Set Artifact Name)
        run: |
          echo "ARTIFACT_NAME=NovaKernel-${{ github.event.inputs.device }}-KSU-$(date +%Y%m%d)" >> $GITHUB_ENV

      - name: 📤 رفع الكيرنل النهائي (Upload)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ~/toolchains/RIO/work_ksu/*
          compression-level: 9
